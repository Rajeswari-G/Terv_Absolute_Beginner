Input: racecar
Output: Yes

#include<stdio.h>
#include<string.h>
int main()
{
char a[100],s[100];
int i,j,n,count=0;
scanf("%[^\n]s",s);
j=0;
for(i=n-1;i>=0;i--)
{
a[j]=s[i];
j++;
}
for(i=0;i<n;i++)
{
if(a[i]==s[i])
{
count++;
}
}
if(count==n)
printf("Yes");
else
printf("No");
return 0;
}
